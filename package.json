{
	"description": "PetalLock",
	"repository": {},
	"license": "Unlicense",
	"type": "module",
	"devDependencies": {
		"@types/node": "22.5.4",
		"@zoltu/file-copier": "3.0.0",
		"better-typescript-lib": "2.8.0",
		"typescript": "5.5.4",
		"funtypes": "5.1.0"
	},
	"dependencies": {
		"@preact/signals": "1.2.2",
		"multiformats": "13.2.2",
		"preact": "10.19.3",
		"viem": "2.18.5"
	},
	"scripts": {
		"setup": "bun install --frozen-lockfile && bun run contracts && bun run vendor && bun run build",
		"contracts": "cd solidity && bun install --frozen-lockfile && bun run compile && cd .. && bun run vendor",
		"test": "bun ./app/ts/tests/test.js",
		"build": "bun --bun tsc",
		"serve": "bun x http-server ./app",
		"vendor": "bun --tsconfig-override tsconfig.vendor.json ./build/vendor.mts"
	}
}
